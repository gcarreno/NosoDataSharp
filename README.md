# Noso Data Sharp

Basic Noso Data blocks in C#

## Build

Make sure you have the `dotnet` command line tool installed.

```console
$ dotnet build src
```

## Test

Make sure you have the `dotnet` command line tool installed.

```console
$ dotnet test src
```
